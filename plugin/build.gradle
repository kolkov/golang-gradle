apply plugin: 'groovy'
apply plugin: 'maven'
version = 1.5
group = 'go.goplugin'
archivesBaseName = 'goplugin'
repositories.mavenCentral()
dependencies {
	compile gradleApi()
	groovy localGroovy()
}

uploadArchives {
	repositories.mavenDeployer {
		repository(url: 'file:../lib')
	}
}